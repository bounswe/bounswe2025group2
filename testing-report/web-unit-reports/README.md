- Opening up the `index.html` under the `human-readable/coverage` folder with a browser enables us to see and navigate between detailed testing and coverage results.

- The `machine-readable` folder contains two `XML` files, one including the detailed coverage analysis, and one demonstrating the test counts for each module.
